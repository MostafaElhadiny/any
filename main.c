#include <stdio.h>
#include <stdlib.h>

int main()
{
    float x, c = 15.72;

    printf("Hello!\n Enter the number in ($)");
    scanf("%f",&x );

    float z = x * c;
    printf("your number in (E G P) is %f",z);

    return 0;
}
